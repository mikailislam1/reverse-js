// string type variable

var bottleContent = 'water';
var bottleColor = 'yellow';

// number type variable

var bottlePrice = 20;

// boolean type variable

var penColor = 'blue';
var isExpensive = true;

// ARRAY

var tableItems = ['bottle', 'keyboard', 'mouse', 'paper', 'router'];
var item3 = tableItems[3];
console.log(item3);

tableItems[4] = 'pen';
var penIndex = tableItems.indexOf('keyboard');
console.log(penIndex);

// push pop

// condition
if(tableItems.length > 4){

}
else if(tableItems[3] = 'water'){

}
else{
    
}